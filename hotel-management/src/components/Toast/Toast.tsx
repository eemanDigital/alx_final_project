"use client";
// A toast is a UI that unobtrusively provides feedback about an operation using a popup
import { Toaster } from "react-hot-toast";

const Toast = () => <Toaster />;

export default Toast;
